# 行动卡统一迭代台账

## 2026-09-08：Gemini 3.8 Flash 适配补齐

依据专项 01 的 `reports/2026/2026-09-07-1031.md` 中 N06 / A07，以及本次用户明确补齐 Gemini 新模型的要求，将目录与离线请求适配单独落实，对应 [PR #713](https://github.com/shiwenwen/hope-agent/pull/713)。上一轮 #710 未实现此型号；本次不将整个 A07 记为完成，也不改写原雷达归档。

| 范围 | 本次实现 | 验证边界 |
| --- | --- | --- |
| Gemini 3.8 Flash 目录 | Google 直连 `gemini-3.8-flash` 与 OpenRouter `google/gemini-3.8-flash` 预设；文本/图片、1,048,576 上下文、65,536 输出及标准报价 | 两个渠道分别核验官方目录；跨服务商补全可发现模型，用户价格仍需确认后覆盖 |
| 官方请求档位 | 精确 Google HTTPS 端点与型号限定 `minimal → low`、`xhigh/max → high`；正常档位与省略参数行为保留 | 合成请求覆盖文本/图片、工具表、档位、伪域名、端口、同名中转和旧型号；没有真实服务调用 |
| 成本估算 | 直连及带渠道前缀 ID 不再落入通用默认价，按 0.75/3.75 美元每百万令牌兜底 | 用户报价优先；Google 促销到 2026-12-31，之后需核验更新；不代表缓存或实际账单 |

A07 的真实模型质量/性能对比、地区和套餐资格、原生音视频/PDF、持久思考签名及其他 Flash / Max 渠道实验继续待验证或授权。官方依据：[Google 模型](https://ai.google.dev/gemini-api/docs/models/gemini-3.8-flash)、[兼容接口](https://ai.google.dev/gemini-api/docs/openai)、[Google 价格](https://ai.google.dev/gemini-api/docs/pricing)、[OpenRouter 目录](https://openrouter.ai/google/gemini-3.8-flash)。

## 2026-09-07：模型适配更新

依据专项 01 的 `reports/2026/2026-09-07-1031.md`（上海时间 10:31），在 `main@77cd06cf33dfe7a0e7ca325b9de196a6d7c21e5d` 上落实 A01–A06。对应 [PR #710](https://github.com/shiwenwen/hope-agent/pull/710)，下表记录本轮实现范围；历史雷达文件与原账户配置保持不变。真实模型、账户权限及计费表现尚未验证。

| 行动卡 | 本轮实现 | 验证边界 |
| --- | --- | --- |
| A01 公共退役预设 | Cerebras 移除 `gemma-4-31b`、`zai-glm-4.7`、`qwen-3-235b-a22b-instruct-2507`、`llama3.1-8b`；Together 改为 `deepseek-ai/DeepSeek-V4-Pro-0813` | 仅更新新建模板，其他渠道同名模型与用户专属部署不受影响 |
| A02 最短重试等待 | `Retry-After` 保留服务端下限；整数溢出饱和、有界解析日期并转为单调计时；超出剩余等待预算返回 `RetryDeferred` | 合成 60 秒提示与 10 秒本地上限、预算不足、跳过等待、取消、日期及畸形值 |
| A03 服务商阻断终态 | HTTP / SSE 精确识别 `misalignment_policy_violation`；主对话及后台一次性调用停止重试、密钥轮换、模型回退和后续工具；保留类型化失败与持久终态标识 | 仅合成错误；默认 `store=false`、不请求持久推理，未启用状态能力 |
| A04 Astra 请求策略 | OpenAI Responses 预设新增 `gpt-6-astra`；仅官方 HTTPS 端点应用档位和采样限制；`max` 保留、`minimal` / `none` 映射 `low`；Chat 工具请求在发送前拒绝并提示协议选择 | 请求构造、后台请求与中转回归；基础价 10/50 美元每百万令牌，只覆盖标准短上下文档 |
| A05 Claude 原始思考 | 新增 Fable 5.1 预设；工具轮与最终轮均保存原始思考、签名、遮蔽块及顺序；官方端点用绑定控制明确请求前缀失配时由服务端舍弃，显示提示并记录原因；本地不删原块 | 合成签名单独增量、摘要加签名、遮蔽块、工具续轮、保留的压缩后缀、前缀变化及切旧模型提示；未验证新旧账户差异 |
| A06 工作区绑定 | 每个鉴权档案新增可选 `anthropicWorkspaceId`；开启后缺失值拒绝保存和发送；共用请求头构造覆盖主对话、一次性请求与连接测试；冷却后的选取、轮换和缓存隔离均遵守声明的工作区 | 无真实密钥的配置、请求头敏感标记、GUI 与档案轮换回归；限定工作区旧密钥继续省略该头 |

A07（新渠道与资格）、A08（多维计价）、A09（运行时升级与模型对比）、A10（Astra 执行协议）仍为实验或待证据范围；本轮不安装运行时、不上传资料、不执行付费模型实验。Fable 的兼容处理能观察到思考被服务端舍弃，不能据此承诺跨前缀或跨模型保留全部推理连续性。大盘价格仍是估算，不包含 Astra 超过 272,000 输入令牌的阶梯、缓存写入、地区加价或实际账单。

本地验证已通过：`ha-agent-runtime` / `ha-dash` / `ha-server` 单点编译；相关 `ha-core` 故障恢复、请求构造、思考历史、工作区及后台终态回归；`ha-agent-runtime` 的 20 项一次性调用回归；前端 3 个文件的 16 项测试、类型检查、12 种语言完整性、用户指南中英文对齐与运行内核边界检查。桌面界面未做视觉验收，服务商真实 API 冒烟未执行。

主要官方依据：[Astra 模型与请求规则](https://developers.openai.com/api/docs/guides/latest-model)、[服务商阻断语义](https://developers.openai.com/api/docs/guides/safety-checks/misalignment-monitoring)、[Claude 思考绑定控制](https://platform.claude.com/docs/en/build-with-claude/preserved-thinking)、[Claude 工作区鉴权](https://platform.claude.com/docs/en/manage-claude/authentication)、[Cerebras 退役清单](https://inference-docs.cerebras.ai/support/deprecation)、[Together 变更记录](https://docs.together.ai/docs/changelog)。

## 2026-08-31：范围与证据

本轮从 `main@b14100692`（`v0.38.0`）核对。上一轮 [PR #674](https://github.com/shiwenwen/hope-agent/pull/674) 已合并，开始检查时没有未关闭 PR。本台账记录工程状态，不改写历史雷达报告，也不把报告建议视为外部操作授权。

本次原始输入共 **68 条记录**：56 张专项行动卡、8 张[实验决策卡](radar-experiment-gates.md)、4 项综合立项。综合立项和实验卡与专项卡有重叠，不能相加宣称有 68 项独立功能。下表以“专项编号 + 原卡编号”去重追踪；综合立项只引用其子项。

已产出报告位于项目的“外部能力技术雷达”归档，读取各目录的 `LATEST.md` / `LATEST.json`：

| 专项 | 成功报告时间（上海） | 行动卡数 |
| --- | --- | ---: |
| 01 LLM / Provider | 2026-08-24 10:14 | 8 |
| 04 Memory / Knowledge / RAG | 2026-08-20 18:09 | 6 |
| 05 MCP / ACP / 扩展 | 2026-08-21 19:10 | 8 |
| 07 本地运行时 / 沙箱 / 工具链 | 2026-08-11 22:11 | 7 |
| 09 发布 / 部署 / 供应链 | 2026-08-25 22:13 | 13 |
| 10 IM | 2026-08-28 10:01 | 9 |
| 11 Design | 2026-08-26 15:09 | 5 |

综合路线图为 2026-08-29 01:08 的首次成功结果。02、03、06、08 没有成功归档，保留证据缺口；不补跑、不回填、不把缺失报告解释为“无变化”。部分专项仍审查旧的 `ebef667b5`，其中的“未实现”必须以当前源码重新裁决。

## 本轮统一计划

| 优先级 / 工作包 | 本轮范围 | 验收与验证 | 风险与回滚 |
| --- | --- | --- | --- |
| P0 安装与凭据 | 关闭无固定字节证明的 Ollama 脚本执行，复用官方手工下载入口；OAuth 改为安全原子写，异步调用经阻塞池，持久化失败不假报登录成功 | 安装入口在联网、提权前拒绝；旧 0644 token 替换后为 0600；失败保留原文件；单点编译与合成回归 | 不更改已安装 Ollama、模型或用户凭据；不以恢复普通写/可变脚本作回滚 |
| P0 发布供应链 | Actions 固定完整提交摘要；R2 密钥只进入实际读写步骤；rclone 执行前校验固定摘要；加入同源本地与 CI 静态守卫 | 零可移动 Action 引用；安装步骤无 R2 密钥；摘要不符不能解压执行；不改 job 名、矩阵、发布布局或保护规则 | 保留原 Action 版本与发布顺序；只回退到已核验摘要；不启用新发布权限 |
| P1 Provider 明确错误 | 修复 Cloudflare 新建模板的端点/模型前缀；清理 Copilot 失效默认候选；修正直连基础报价；补齐已知模型请求档位边界 | URL、模板和请求形状契约；已有用户配置与网关报价不被迁移覆盖；不运行真实模型 | 按官方证据限定端点/模型，未知兼容端不套用原厂规则；价格有效期显式登记 |
| P1 状态与支持范围 | 56 张专项卡逐项登记证据、缺口和重新评估条件；校正 Intel Mac 支持声明；8 张实验卡保持原门控 | 台账无遗漏、无“本周期完成=已启用”的混淆；中英文说明一致；关联 PR/Issue 不重复处理 | 历史报告保持不可变；不能把部分交付写成整卡完成 |
| 交付 | 一个 `codex/` 分支，提交带 🦭，正式 PR，处理所有已知审查与 CI 问题后合并 | push 使用原 pre-push 全门禁；不另跑全套测试或评测；最新提交复审、零未解决线程、required checks 全绿、确认 MERGED | 有新增权限、费用、凭据或产品方向决策的子项单独停止，不扩大授权 |

开发只做相关 `cargo check` / `pnpm typecheck` 与轻量静态校验；回归测试随实现提交，由 pre-push/CI 统一执行。OpenAI 价格和请求契约按 OpenAI Docs 核验官方来源；其余沿已有报告来源做必要的事实复核，不重新开展雷达调研。

## 专项行动卡状态

“已交付”只指表中明确的本地实现；真实服务的成功率、费用和性能指标没有运行证据时不得宣称通过。“待决策”不是技术阻塞的替代词：它专用于需要选择新增产品能力或改变既有合同的事项。“待授权”不自动启动任何实验。

### 01：LLM / Provider（8 张）

| 原卡 | 当前状态 | 证据、剩余范围与重新评估条件 |
| --- | --- | --- |
| `lifecycle_guard` | 部分交付 | `templateHygiene.test.ts` 已守住 OpenAI/DeepSeek/MiMo/Hy3 退役别名；补 Copilot 默认候选。Chutes 匿名目录不能证明账户可用性；实时目录与全局生命周期状态须先冻结账户/离线语义 |
| `cloudflare_rest_migration` | 已交付 | 仅新建模板改官方 REST 路径，不改已有网关配置或申请 Token；Workers AI 所需网关 header 不假装由单 Token 自动满足 |
| `model_scoped_request_policy` | 部分交付 | Claude 5 自适应思考已在 #674 交付；补 Gemini/Grok 已知请求边界。通用可调能力模式需另定 GUI/设置/兼容方案，不新增未验明字段 |
| `price_and_alias_hotfix` | 已交付 | 旧别名清理已交付；补 Sol / Sonnet 5 直连基础价，不机械改网关报价，不把估算当账单 |
| `actual_and_tiered_cost` | 待决策 | 需冻结实际金额、币种、来源、历史账单重算与网关计费优先级；当前没有授权账单样本，不能承诺 1% 对账误差 |
| `responses_state_and_cache_benchmark` | 待授权 | 归入现有 Responses 实验门；缺批准的数据、端点、费用与停止条件；#682 的调用身份修复不是 A/B 性能证据 |
| `anthropic_files_experiment` | 待授权 | 缺上传/保留/删除范围与费用批准；不上传文件，不增加远端附件生命周期 |
| `credential_and_compliance_metadata` | 待决策 | 不把地区/ZDR/TEE 自报字段当合规证明；新增到期策略和过滤规则须确认产品语义，组织管理权限不申请 |

### 04：Memory / Knowledge / RAG（6 张）

| 原卡 | 当前状态 | 证据、剩余范围与重新评估条件 |
| --- | --- | --- |
| A1 嵌入日志 | 已交付 | #674 的元信息日志与合成回归；机器已随 #678 移至 `ha-memory`，不恢复正文日志 |
| A2 嵌入用途 | 已交付 | `EmbeddingPurpose`、签名/缓存隔离、显式重嵌已交付；迁移位置变化不等于能力缺失 |
| A3 外部版本门 | 已交付 | 所有者显式探测、凭据指纹绑定与写前复验；未知版本和未授权写入继续拒绝 |
| A4 远端接口回归 | 已交付 | #674 本地协议回归与版本门；真实端点兼容只在所有者授权后验证 |
| A5 检索证据套件 | 已交付 | `evals/suites/knowledge-retrieval-evidence`；存在套件不代表本轮重跑或取得真实模型成绩 |
| A6 图谱影子评估 | 待授权 | 候选/轨迹基础已交付；无新召回收益证据，不将其注入提示词；沿实验门重新评估 |

### 05：MCP / ACP / 扩展（8 张）

| 原卡 | 当前状态 | 证据、剩余范围与重新评估条件 |
| --- | --- | --- |
| A1 MCP 双栈 | 已交付 | #674 的 `rmcp 3.x` 兼容基础；远端 Tasks 产品投影仍受实验门约束 |
| A2 ACP 双工 | 已交付 | `ha-acp` 的 v1、权限/问答与流式协议；不因上游 schema 小版本重复立项 |
| A3 ACP 分发 | 已交付 | `acp_control/registry.rs` 拒绝缺分发描述，启动参数不猜测；环境白名单与未注册进程清理已交付 |
| A4 工作区信任 | 已交付 | Hooks 逐工作区与内容摘要授权、最小环境；旧全局开关不恢复授权 |
| A5 技能方言 | 已交付 | #674 项目发现与显式权限上限；标准 `allowed-tools` 不授予额外权限 |
| A6 扩展来源/认证代理 | 待决策 | 现有分发描述不等于跨扩展认证代理；跨 Skills/Hooks/MCP 的凭据代理是新增权限面，需先确定身份和确认合同 |
| A7 Registry/远端任务 | 待授权 | 复用 MCP 实验卡；缺两个独立授权服务器及取消/恢复证据，不新建本地后台状态域 |
| A8 A2A | 观察 | 按既有决策主动不做；只有真实伙伴、出站用途、威胁模型及预算齐全才重新评估 |

### 07：本地运行时（7 张）

| 原卡 | 当前状态 | 证据、剩余范围与重新评估条件 |
| --- | --- | --- |
| HA-RT-001 宿主基线 | 待授权 | 不升级本机 OS/Chrome/Docker/工具；需所有者确认具体宿主和版本 |
| HA-RT-002 Ollama 试运行 | 待授权 | 不下载模型、不启动真实推理；先批准测试机器、模型和资源预算 |
| HA-RT-003 Office Python | 待授权 | 保留只读诊断；不改系统 Python 或安装 Office/视觉工具 |
| HA-RT-004 LSP | 待授权 | 缺明确安装目标；保留未安装时的降级，不安装全局工具 |
| HA-RT-005 浏览器/FFmpeg 清单 | 已交付 | #674 不可变 URL/大小/摘要、原子提升与跨进程安装锁 |
| HA-RT-006 沙箱镜像 | 已交付 | 固定多架构摘要、Docker `isolated` 双重拒绝与资源边界 |
| HA-RT-007 工具链诊断 | 已交付 | 只读 Toolchain Doctor 已交付；不把检测结果变成自动升级 |

### 09：发布与供应链（13 张）

| 原卡 | 当前状态 | 证据、剩余范围与重新评估条件 |
| --- | --- | --- |
| A-01 Ollama 安装 | 已交付 | 采用报告允许的安全降级：手工安装；重开前必须有固定版本、大小、摘要及二阶段下载完整性证明 |
| A-02 R2/rclone | 部分交付 | 密钥下沉与字节校验；受保护环境、前缀密钥和临时认证代理需额外权限，不在此轮配置 |
| A-03 Actions | 部分交付 | 固定代码摘要并加入静态守卫/版本更新配置；仓库级安全告警开关另需管理员授权 |
| A-04 OAuth 安全写 | 部分交付 | 使用现有跨平台安全原语并收口异步写入；Windows 原语当前继承 DACL，不冒称已实现新的专属 ACL |
| A-05 Intel Mac | 已交付 | `v0.38.0` 资产仍无 x64 macOS；修正支持声明，不补跑 `v0.35.0` 历史构建、不假定有可用安装包 |
| A-06 平台可信签名 | 待授权 | 缺 Developer ID/公证/Authenticode 凭据与费用批准；不关闭系统安全机制 |
| A-07 不可变发布 | 待决策 | 与“发布后补齐 Intel 资产”流程冲突；需先决定必需平台矩阵与不可变发布策略，不能擅改仓库开关 |
| A-08 Linux 双钥 | 待授权 | 当前密钥到期在 2027-05；新密钥、分发与重叠信任窗口须所有者批准；2026 Q4 前重新评估 |
| A-09 glibc 下限 | 待决策 | 需确定最低支持发行版和最终打包库的证据范围；不能仅凭构建机版本伪造精确最低版本 |
| A-10 OAuth 回调 | 待证据 | 当前仅监听 `127.0.0.1`，PKCE/state 已有；未证明注册客户端允许替换 redirect/临时端口，不盲改登录协议 |
| A-11 Docker 离线交付 | 待决策 | 现有双架构来源证明保留；OCI 离线包、SBOM 的正式发布范围与基础镜像更新策略须冻结，不额外发布容器 |
| A-12 镜像新鲜度 | 待决策 | 报告的“客户端选最高版本”与仓库明确契约冲突；如需外部健康监控，先确认部署位置、告警与操作权限 |
| A-13 Docker Scout | 观察 | 仓库无 Dashboard Policies 集成；只有所有者确认在外部使用后才迁移，不猜测团队外部状态 |

### 10：IM（9 张）

| 原卡 | 当前状态 | 证据、剩余范围与重新评估条件 |
| --- | --- | --- |
| A1 Slack 原生停止 | 待授权 | 新原生事件需确认订阅权限与测试账户；当前文本 Stop 不等于 Agent Sessions 原生停止 |
| A9 Telegram 原生停止 | 待授权 | 需授权 bot 和官方事件样本，绑定精确会话世代后才能启用；不以文本命令冒充原生事件 |
| A3 Google Markdown/来源 | 待授权 | 标准 Markdown 已交付；原生来源预览缺测试租户/真实来源映射证据，任意 Markdown 链接不晋升为可信引用 |
| A4 Discord 隐藏频道 | 已交付 | #674 可选能力、隐藏父线程过滤和恢复回归；不再重复加入同一功能 |
| A8 Google 消息动作 | 待授权 | 需要控制台动作配置与测试空间；不得创建未知动作或扩大现有 JWT/scope 授权 |
| A2 iMessage 协商恢复 | 已交付 | `initialize/status`、投递分类、溢出追赶与旧协议回退已在 #674 交付 |
| A5 Discord 文件请求 | 已交付 | #674 账号级默认关闭、受限文件输入、实际字节/大小与精确会话绑定 |
| A6 Signal/WhatsApp | 已交付 | #674 只读运行时快照、元数据事件过滤和有界并发探测 |
| A7 Teams | 观察 | 需求门不变：设计伙伴与活跃度或明确合同；不申请租户权限 |

### 11：Design（5 张）

| 原卡 | 当前状态 | 证据、剩余范围与重新评估条件 |
| --- | --- | --- |
| A1 Figma 往返 | 已交付 | #674 双版本守卫与不确定投递禁止重试；真实账号成功率仍需授权样本，不重新开发已存在往返 |
| A2 场景与视觉回归 | 已交付 | #674 有界场景、单活动 iframe、隔离 CDP 截图/视口恢复；新质量指标不在本轮补跑 |
| A3 版本化配方 | 待决策 | 当前静态内置配方不等于版本化可安装工作流；先确认导入、品牌、版本升级和授权语义 |
| A4 页面能力代理 | 待决策 | iframe 零能力默认不变；页面调用本地数据/写入属于新增产品与权限面，需先批准最小能力集合 |
| A5 固定版本评审 | 已交付 | #674 查看/评论角色、撤销、版本锚定与所有者新版本写；实时源码共编仍主动不做 |

## 聚合与下次检查

专项卡合计 **56 张**：已交付 **24**、部分交付 **5**、待授权 **14**、待决策 **9**、待证据 **1**、观察 **3**。其中本轮新增/补齐 9 张，其余已交付项来自既有合并；部分交付卡的剩余条件仍有效，不能整体勾选完成。

| 综合立项 | 本轮状态 | 对应剩余条件 |
| --- | --- | --- |
| P1 发布可信链 | 部分交付 | 签名凭据、平台支持与外部权限策略 |
| P2 模型生命周期/请求/计费 | 部分交付 | 账户目录、账单语义与受控实验 |
| P3 IM 原生停止/来源 | 待授权 | 订阅权限、授权测试账户及事件样本 |
| P4 Design 配方/页面能力 | 待决策 | 导入/升级语义与最小权限集合 |

关联 Issue 已核对正文与维护者现有结论：[#421](https://github.com/shiwenwen/hope-agent/issues/421) 与 [#423](https://github.com/shiwenwen/hope-agent/issues/423) 已由 #425 修复；[#327](https://github.com/shiwenwen/hope-agent/issues/327) 的配置能力与入口说明已有 #330；[#535](https://github.com/shiwenwen/hope-agent/issues/535) 的永久替换图片建议违反保留权威历史的契约；[#653](https://github.com/shiwenwen/hope-agent/issues/653) 及后续安装包高亮修复已有 #654 / #681。均不重复改动或自动关闭。#678 的拆分、#682 的调用身份修复和 #684 的侧聊已在当前基线，不另行实现。

综合 P1 映射 09，P2 映射 01，P3 映射 10 的原生停止/来源，P4 映射 11-A3/A4；不再建重复任务。8 张实验卡维持“本周期决策完成”，不意味着实验已运行或功能已开放。

下次优先检查本轮 PR 是否已合并，再查看已有新报告、以上授权/决策是否改变。价格在 2026-11-21 前重新核验；Linux 密钥在 2026 Q4 前确定重叠迁移；没有新增证据则不制造新功能或重新提交已完成卡。

## 本轮交付证据

代码与文档在 [PR #689](https://github.com/shiwenwen/hope-agent/pull/689) 交付；最新审查、CI 和合并状态以该 PR 的权威记录为准，不声明外部实验或权限控制完成：

- 凭据：`ha-core/src/oauth.rs`、`ha-base/src/platform/`、HTTP 登录调用点；合成回归覆盖完整替换、旧权限收紧、目录/链接拒绝。浏览器回调页只确认收到回调，不提前宣布登录成功。
- 安装：`ha-local-llm/src/local_llm/` 关闭不具备完整字节证明的安装脚本；保留历史任务，旧重试在联网/提权前拒绝，已装服务不变。
- 供应链：所有外部 Action 固定完整提交摘要，R2 密钥仅进读写步骤，预检传布尔值；rclone 固定校验后才解压。静态守卫及合成用例同时接入 pre-push / CI；Wrangler 安装不持上传凭据。
- 模型：官方端点/精确模型请求档位、Cloudflare 新建模板、Copilot 默认候选、Sol/Sonnet 5 直连基础报价；用户既有配置不自动覆盖。中英文安装/平台支持说明同步。
- 正常 push 已完整通过 pre-push：格式检查、clippy、内部特征编译检查、类型检查、lint、架构守卫、前端 281 个文件 / 1603 个测试与全部规定 Rust 测试；新增供应链回归 5/5。国际化缺失/多余/插值/排序均为零，10 个工作流 YAML 解析通过。两个既有前端 Hook 警告无新增错误。未补跑报告、未跑真实模型或付费实验。
- PR 复审补齐两项同范围缺口：模型任务所有前端安装/重新下载/重试入口实时检查手工安装前提；工作流守卫改为结构解析，防止行内写法漏检。新增手工安装与对话框回归 11 项，供应链回归扩至 9 项；定向验证通过。完整门禁及最新提交复审结果继续以 PR 记录为准，行动卡交付范围与授权门不变。
